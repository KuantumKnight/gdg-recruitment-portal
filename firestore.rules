rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The Next.js server uses Firebase Admin SDK and enforces authorization in route handlers.
    // Client-side Firestore access is intentionally disabled so applicants cannot bypass those checks.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
